apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-auth-server.fullname" . }}
  labels:
    {{- include "node-auth-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-auth-server.selectorLabels" . | nindent 4 }}
